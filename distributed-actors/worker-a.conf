# Dedicated config for worker-a.

mode = "worker"
host = "127.0.0.1"
port = 45500
name = "worker-a"
zone = "zone-a"
lifetime = 0

caf {
  middleman {
    enable-automatic-connections = false
    app-identifiers = ["distributed-actors-codex"]
    heartbeat-interval = 0ms
  }
  logger {
    console {
      format = "[%c:%p] %d %m"
      verbosity = "info"
    }
  }
}
